spring:
 application:
  name: one
 cloud:
                         config:
                          label: config-server
                          discovery:
                           enabled: true
